"use client"

import React from 'react'
import SiteMaintenance from './SiteMaintenance'

export default function SiteMaintenanceClient({ page }: { page: string }) {
  return <SiteMaintenance page={page} />
}
